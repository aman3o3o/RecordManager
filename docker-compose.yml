version: "3.9"
services:
  frontend:
    build:
      context: ./frontend
    env_file:
      - ./frontend/.env.docker
    ports:
      - "3000:80"
  
  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:3000"
